
let age =prompt('Enter Your age Plz...');


if(age==21){
        console.log(`Your age is ${age}. & In bangladesh, ${age} is the stating age for the marriage for male...`);
}else if(age>=21){
        console.log(`Your age is ${age}.Now You can get marriage if u want. u should get married before ${age-21} 
years ago...`);
}else{
        console.log(`Your age is ${age}.& Ur age is not prefect for the marriage. just Wait only ${21-age} years then u 
can marriage who ever u want...`);
};